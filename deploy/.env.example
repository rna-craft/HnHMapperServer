# HnH Mapper Server - Environment Variables Template
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: Never commit .env to version control!
# The .env file contains sensitive credentials.

# ============================================================================
# Docker Registry Configuration
# ============================================================================
# GitHub Container Registry upstream repository
# Format: username/repository (e.g., myuser or myorg)
UPSTREAM=your-github-username

# ============================================================================
# Caddy & Cloudflare Configuration
# ============================================================================

# Your domain name (required for HTTPS)
# Example: example.com or subdomain.example.com
CADDY_DOMAIN=yourdomain.com

# Cloudflare API Token (required for DNS-01 ACME challenges)
#
# To create a Cloudflare API token:
# 1. Log in to Cloudflare Dashboard (https://dash.cloudflare.com/)
# 2. Go to My Profile → API Tokens
# 3. Click "Create Token"
# 4. Use the "Edit zone DNS" template
# 5. Under "Zone Resources", select:
#    - Include → Specific zone → yourdomain.com
# 6. Click "Continue to summary" → "Create Token"
# 7. Copy the token and paste it below
#
# Required permissions:
# - Zone:DNS:Edit
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here

# Email address for Let's Encrypt notifications
# You'll receive expiration warnings at this address (though Caddy auto-renews)
CLOUDFLARE_EMAIL=admin@yourdomain.com

# ============================================================================
# Optional: Build Metadata (for local builds)
# ============================================================================
# BUILD_VERSION=1.0.0
# BUILD_COMMIT=abc123
# BUILD_DATE=2025-01-01T00:00:00Z
